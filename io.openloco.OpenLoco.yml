app-id: io.openloco.OpenLoco
runtime: org.freedesktop.Platform
runtime-version: '22.08'
sdk: org.freedesktop.Sdk
command: OpenLoco
rename-appdata-file: openloco.appdata.xml
rename-desktop-file: openloco.desktop
rename-icon: openloco
copy-icon: true
sdk-extensions:
    - org.freedesktop.Sdk.Compat.i386
    - org.freedesktop.Sdk.Extension.toolchain-i386
finish-args:
    # X11 + XShm access
    - --share=ipc
    - --socket=x11
    - --socket=pulseaudio
    # Network access - for multiplayer
    - --share=network
    # OpenGL access
    - --device=dri
    # 32 bit application
    - --allow=multiarch
add-extensions:
    - org.freedesktop.Platform.Compat.i386:
        - directory: lib/i386-linux-gnu
          version: '22.08'   
    # This is not strictly required, but needed for debugging 32 bit programs
      org.freedesktop.Platform.Compat.i386.Debug:
        - directory: lib/debug/lib/i386-linux-gnu
          version: '22.08'
          no-autodownload: true

modules:
    - name: OpenLoco
      buildsystem: cmake-ninja
      builddir: true
      build-options:
        - cxxflags: -m32 -Wno-error=null-dereference  
      config-opts: 
        - -DOPENLOCO_USE_CACHE=OFF
        - -DOPENLOCO_BUILD_TESTS=OFF
      sources:
        - type: git
          url: https://github.com/garethppls/OpenLoco.git
          commit: a22a1cc61c5431b540cf2a37bb15a6e2a794421f